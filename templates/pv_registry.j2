apiVersion: v1
kind: PersistentVolume
metadata:
  name: registry
spec:
  capacity:
    storage: 100Gi 
  accessModes:
  - ReadWriteMany
  nfs: 
    path: /exports/registry
    server: "{{ hostvars['bastion'].host_ip }}"
  persistentVolumeReclaimPolicy: Retain
  
